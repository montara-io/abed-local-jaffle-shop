-- AUTO GENERATED BY MONTARA
--UPDATED: 2025-08-06T12:20:15.643Z
--<DBT_CODE>

WITH raw_listings AS ( SELECT * FROM {{ source('raw_listings', 'raw_listings') }})

select
h.id,
h.name,
h.host_id,
h.updated_at,
h.created_at
from
raw_listings h
limit
200
order by
h.created_at
--</DBT_CODE>
--<ORIGINAL_CODE>
--select
--  h.id,
--  h.name,
--  h.host_id,
--  h.updated_at,
--  h.created_at
--from
--  raw_listings h
--limit
--  200
--order by
--  h.created_at;
--</ORIGINAL_CODE>